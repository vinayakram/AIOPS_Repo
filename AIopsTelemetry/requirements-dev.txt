# ── Test runners ──────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
httpx>=0.27.0               # FastAPI async test client (already in requirements.txt)

# ── Linting & formatting ──────────────────────────────────────────────────────
ruff>=0.4.0
black>=24.4.0

# ── Type checking ─────────────────────────────────────────────────────────────
mypy>=1.10.0
types-requests>=2.32.0      # stubs for requests (used in aiops_sdk)

# ── Test utilities ────────────────────────────────────────────────────────────
freezegun>=1.5.0            # freeze time.time() / datetime.utcnow() in tests
respx>=0.21.0               # mock httpx calls (webhook dispatcher tests)
